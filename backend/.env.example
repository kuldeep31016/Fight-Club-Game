# Port the HTTP + Socket.IO server listens on.
PORT=3001

# Comma-separated list of allowed browser origins for CORS.
# Use your Vercel URL in production, e.g. https://fightcam.vercel.app
CLIENT_ORIGIN=*
